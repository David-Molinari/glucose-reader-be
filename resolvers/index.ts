import { RangesResolver } from './RangesResolver';
import { DataResolver } from './DataResolver';

/**
 * @description holds all resolvers
 */

export const resolvers = [RangesResolver, DataResolver];
